# Phase 0: Viper Environment Dependencies
# Core Mamba dependencies (from pyproject.toml)
torch>=1.12.0
triton
ninja
einops
transformers
packaging
setuptools>=61.0.0

# Viper-specific Phase 0 dependencies
muon  # Muon optimizer for matrix weights
jax[cuda]  # JAX with CUDA support for prototype implementation
alpa  # Distributed training framework
flash-attention==2.5.2  # Specific version required

# Optional Mamba dependencies
causal-conv1d>=1.2.0

# Development dependencies
pytest
hypothesis  # Property-based testing per CLAUDE.md requirements

# Additional dependencies for formal verification
# Note: Lean 4 installation separate - see setup_lean.sh